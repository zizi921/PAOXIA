# PAOXIA 跑下｜MVP 实现方案与 Build Steps

更新时间：2026-09-25

状态：开发前方案。产品范围来自已确定的 Build Brief；本文中的异常处理规则、技术选型和验收安排是建议默认方案。个人主体准入、实际类目、备案路径、云端费用与最终视觉仍需核实。

## 1. 项目现状与产品目标

当前项目处于方案与视觉探索阶段，尚无业务代码。概念图草案保存在 `output/imagegen/`；用户选定的小人原图保存在 `assets/characters/paoxia-runner-approved.png`。

让用户轻松记录一次跑步，同时留住沿途的见闻和感受，不被配速、成绩和打卡压力主导。

平台为微信小程序，计划以个人主体发布，先服务自己和一小群同好。全中文，文案简短、轻松、不催促。视觉方向为粗拙手绘、黑白线条、大留白、少量浅绿色。用户已选定提供的小人原图 `assets/characters/paoxia-runner-approved.png`：斜向跑动、圆头短发、圆耳、夸张前伸的大鞋，以及自然不均匀的黑色线条。后续界面使用此形象，延展动作时保持造型和手绘笔触一致；不使用此前生成的人物草案。用户强调不要照搬其他作品。具体页面布局、字体和其他图标仍待确认。

### MVP 范围

- 开始、暂停、继续、结束跑步，自动计算时长。
- 距离手填，可留空。
- 跑后可选心情、见闻图标和一句话，全部跳过也能保存。
- 历史列表、单次详情、修改和删除。
- 本地暂存、联网同步；同一微信账号换手机可找回已同步记录。
- 默认仅自己可见，不额外要求手机号、昵称或头像。
- 处理锁屏、切换应用、意外退出、断网和忘记结束，清楚区分待同步与已同步。

### 不在范围内

GPS 路线、自动测距、配速和专业运动分析；照片、自由涂鸦、AI 内容生成；训练计划、连续打卡、排行榜、积分、角色养成；公开动态、关注、评论等社区功能；付费功能。

跑步途中不安排观察任务，不要求操作手机，也不要求搭配其他运动软件。

## 2. 最小技术方案

采用原生微信小程序 + 微信云开发，先本地可靠保存，再同步云端。开发顺序先验证计时恢复和数据可靠性，再完成手绘视觉。

| 部分 | 建议 |
| --- | --- |
| 前端 | 原生小程序，TypeScript + WXML + WXSS |
| UI | 原生组件、自定义简单样式、随包发布的原创插画和图标 |
| 本地保存 | 小程序 Storage，保存当前跑步、表单草稿、历史缓存和待同步操作 |
| 云端 | 微信云开发云函数 + 文档数据库 |
| 身份 | 云函数获取微信身份，不自建密码、手机号登录 |
| 数据访问 | 全部通过云函数，客户端禁止直接读写数据库 |
| 同步触发 | 保存后、回到前台、网络恢复、手动重试 |
| 环境 | 开发与正式数据隔离；发布前建立独立正式环境 |

无需自建服务器、实时数据库监听或每秒云端计时。前端分为计时状态、本地存储、同步、页面四个核心模块，计时和同步逻辑不分散到各页面。

微信云开发链路由云函数通过 `getWXContext()` 获取可信的 `OPENID`，无需自行维护登录 token。记录归属必须取自服务端身份，不能相信客户端传来的用户标识。

参考：[在微信小程序中调用 CloudBase 云函数](https://docs.cloudbase.net/recipes/add-cloud-function-wechat-miniprogram)。

## 3. 计时与恢复

### 状态流转

```text
未开始 → 跑步中 ⇄ 已暂停 → 跑后草稿 → 已保存
                              ↓
                         返回继续跑
```

每次开始、暂停、继续、结束，都先持久化状态，再显示操作成功。前台每秒刷新只是显示效果，不以定时器触发次数累计时长。

```text
自动时长 = 结束时间或当前时间 − 开始时间 − 暂停总时长
```

处于暂停状态时，当前尚未结束的暂停区间也要扣除。锁屏、切换应用或进程退出后，重新打开根据时间点恢复，不依赖后台定时器持续执行。

### 建议默认规则

| 情况 | MVP 处理 |
| --- | --- |
| 锁屏、切换应用 | 保持原来的跑步或暂停状态，不自动暂停 |
| 意外退出 | 下次打开恢复本地状态；未完成时首页显示「接着这次」 |
| 点击「跑完了」 | 固定结束时间并保存草稿，进入跑后记录 |
| 误触结束 | 保存前可「返回继续跑」；把填写表单的间隔记为暂停 |
| 填写中退出 | 恢复草稿，不丢失已填写内容 |
| 忘记结束 | 支持修改结束时间或实际时长，不擅自猜测 |
| 异常长时间 | 例如超过 6 小时，下次打开提醒确认；阈值仅用于提醒，不自动截断 |
| 手机时间被调整 | 对负数、明显跳变提示修正；无法保证离线设备时钟始终准确 |
| 本地写入失败 | 明确提示未保存，不显示成功、不清掉原状态 |

修正时保留原始计时信息，另外记录修正值。显示时优先采用用户确认的时长，不伪造暂停记录来凑时长。

恢复承诺：未完成跑步在原手机恢复；已同步记录可换手机找回。MVP 不做跨设备接力计时。本地尚未同步的数据无法承诺在清除小程序数据或丢失手机后恢复。

## 4. 本地保存与云端同步

### 保存顺序

1. 校验字段。
2. 将记录与待同步操作一起写入本地。
3. 显示「记下了」，进入详情。
4. 后台尝试同步。
5. 收到云端确认后，才标记「已同步」。

本地保存成功不等于云端同步成功。

| 状态 | 文案 |
| --- | --- |
| 本地保存，尚未上传 | 已存本机 · 待同步 |
| 正在请求 | 同步中 |
| 当前版本已被云端确认 | 已同步 |
| 请求失败 | 已存本机 · 同步失败，点此重试 |
| 离线删除，云端尚未确认 | 待删除同步 |

### 必须实现的可靠性规则

- 固定记录 ID：开始跑步时生成，重试保存不会产生两条记录。
- 固定操作 ID：请求超时后重试同一次操作，云端只执行一次。
- 版本号检查：修改带上已知云端版本；两台设备改同一条时，保留本地内容并提示选择，不按手机时间静默覆盖。
- 删除标记：删除后清除正文，保留最小删除标识和版本，防止另一台旧设备把记录上传回来。

云端将版本检查、写入和操作回执放在事务内完成。客户端逐条串行处理待同步操作；同步期间再次修改，旧请求的成功回执不能把新修改误标为已同步。

首版历史拉取使用分页全量核对，不建设复杂增量同步。拉取结果不能覆盖本地尚未上传的修改；只有完整拉取成功，才能判断云端记录是否缺失。

参考：[CloudBase 服务端数据库事务](https://docs.cloudbase.net/database/transaction)。

## 5. 数据结构

| 数据 | 核心字段 |
| --- | --- |
| 跑步记录 `runs` | ID、归属、开始/结束时间、暂停区间、自动时长、可选修正时长、距离、心情、见闻、短句、创建/修改时间、版本、删除标记 |
| 账号元数据 `accounts` | 归属、数据代次、删除处理状态；不保存昵称头像 |
| 同步回执 `mutations` | 操作 ID、归属、结果版本，用于重复请求去重 |
| 本地当前跑步 | 运行状态、时间点、未保存表单 |
| 本地记录缓存 | 记录内容、云端版本、本地修改版本 |
| 本地待同步操作 | 操作 ID、记录 ID、操作类型、提交内容、基础版本、重试状态 |

### 字段约定

- 时长用整数毫秒，距离用整数米；界面输入公里，空白保存为 `null`。
- 心情单选、见闻多选，都允许不选。
- 短句建议最多 100 字。
- 图标保存稳定的枚举 ID，不直接保存展示文案。
- 跑步时间由设备记录；云端创建、修改时间由服务端生成。
- 本地数据带 `schemaVersion`，以后升级能迁移。
- 本地缓存按已确认的账号隔离，未确认身份的离线草稿不得自动归给其他账号。
- 不采集跑步轨迹，不保存照片。

首批图标可以先用占位：心情「轻松、平静、有点累、说不上来」；见闻「树、风、云、猫、路灯、没留意」。先验证文案感受，再制作最终素材。

## 6. 页面与用户流程

```text
首页 → 去跑跑 → 自动计时，可暂停和继续 → 跑完了
     → 可选填写心情、见闻、短句和距离 → 保存 → 本次详情或历史记录
```

| 页面 | 最小行为 |
| --- | --- |
| 首页 | 角色、短句、「去跑跑」、历史入口；有未完成跑步时优先恢复 |
| 跑步中 | 大号时长、暂停/继续、「跑完了」 |
| 跑后记录 | 确认或修正时长，填写可选信息，「保存」 |
| 我的跑步 | 日期、时长、生活片段、同步状态；距离缺省不占位 |
| 单次详情 | 完整记录、修改、删除、同步状态 |
| 隐私与数据辅助页 | 数据说明、同步重试、数据导出、删除全部数据 |

修改记录复用跑后表单，不再做一套编辑页。保存成功用 Toast，无需成功页。

视觉使用黑白、大留白、浅绿和用户选定的小人原图搭建，再完善图标和版式。用户在比较彩色扁平方案后明确要求换回黑白手绘，当前采用 `output/imagegen/paoxia-concept-v3.png` 作为页面概念方向；`paoxia-concept-v4-color.png` 仅保留为未采用的探索稿。保留浅绿按钮与选中提示，不采用彩色人物和大面积彩色色块。

## 7. 隐私、删除与运维

### 首次使用

建议简短说明：

> 跑步先存在这台手机，联网后同步到你的微信账号，仅自己可见。

同时提供完整隐私说明，说明处理的身份标识、跑步和文字数据，以及删除方式。是否需要微信专门的隐私授权流程，要在实际 API 清单确定后核对，不额外索取手机号、昵称或头像。

数据库禁止客户端直连后，云函数仍须逐次检查记录归属。云函数具有更高数据库权限，不能依赖客户端安全规则代替业务鉴权。

参考：[CloudBase 数据隔离与云函数鉴权说明](https://docs.cloudbase.net/recipes/secure-database-multi-tenant-rules)。

### 删除全部数据

建议要求联网，并实现以下流程：

1. 云端完成删除后，才显示「已删除」。
2. 递增账号的数据代次，使旧设备队列失效，避免重新上传旧记录。
3. 若请求中断，下次查询处理结果并继续。
4. 清理本机缓存；其他设备联网后再清理其缓存。
5. 备份恢复必须重新应用删除信息，不能让已删除内容恢复上线。

### 备份与预算

云端同步不等于备份。发布前确定备份保留期、访问权限和恢复步骤，并做一次恢复演练；用户导出可以用 JSON，不增加复杂文件格式。

预算先按 5–10 人试用设计，不承诺免费。云开发套餐和资源计费存在不同版本，应按实际环境确认月费、超量规则、告警和到期处理。

参考：[云开发套餐及资源包说明](https://cloud.tencent.com/document/product/876/136006)。

## 8. Build Steps 与逐步验收

每一步完成并验证后，再进入下一步。

| 步骤 | 交付内容 | 验收方式 |
| --- | --- | --- |
| 0. 发布可行性核实 | 个人主体、实际服务类目、备案路径、名称、云开发开通与费用确认表 | 在微信后台核对真实可选类目；如描述边界不明，向平台核实并留存结果 |
| 1. 最小真机骨架 | 原生项目、页面占位、独立云开发测试环境、身份探针 | 真机打开；云函数能识别调用账号；两个账号身份不同 |
| 2. 可靠计时核心 | 开始、暂停、继续、结束、时间点持久化 | 运行 10 分钟、暂停 3 分钟、再运行 5 分钟，结果 15 分钟；锁屏和强退后恢复一致 |
| 3. 纯本地完整闭环 | 跑后表单、保存、列表、详情、编辑、删除、草稿恢复 | 飞行模式下完成全流程；全部可选项留空可保存；写入失败不误报成功 |
| 4. 云端存取与隔离 | 云函数、数据校验、归属检查、分页读取 | A 账号无法读取、修改或删除 B 的记录；换手机可读取 A 已上传记录 |
| 5. 可靠同步 | 待同步队列、去重、版本冲突、删除标记、状态反馈 | 上传成功但响应丢失时不重复；离线修改恢复联网后正确上传；旧设备不复活删除记录 |
| 6. 异常恢复与数据管理 | 忘记结束修正、误触恢复、隐私说明、导出、删除全部、迁移 | 两台设备冲突不丢内容；全部删除后旧队列失效；旧版本缓存升级不丢失 |
| 7. 最终视觉与中文文案 | 按概念图完成角色、图标、样式和反馈 | 小屏真机无溢出；按钮易操作；跑步中无需额外任务；同步状态容易理解 |
| 8. 小范围试用与发布 | iOS/Android 回归、备份演练、用量告警、审核资料 | 5–10 人试用约一周，核心故障场景通过，严重数据问题归零后提审 |

第 0 步是实际投入前的门槛。备案制度覆盖小程序分发场景，但 PAOXIA 的准确类目及当前个人主体准入尚未核实通过。方案调研时微信部分官方页面无法读取，必须以微信后台和平台答复为准。

参考：[工业和信息化部关于开展移动互联网应用程序备案工作的通知](https://www.gov.cn/zhengce/zhengceku/202308/content_6897341.htm)。

## 9. 发布核心验收标准

- 锁屏、切后台、强退不会让已持久化的计时状态消失。
- 无网也能完成记录，下次打开仍在。
- 重复请求不会多出记录，删除不会被旧设备复活。
- 「已同步」只代表当前版本确实已收到云端确认。
- 同一微信账号能在另一台手机找回已同步记录，其他账号不能访问。

## 10. 待确认事项

- 个人主体适用的服务类目、备案和审核要求。
- 产品名称、页面概念图、最终中文文案和首批图标；小人形象已由用户选定，见 `assets/characters/paoxia-runner-approved.png`。
- 异常长时间提醒阈值，以及时长修正交互。
- 实际 API 对应的隐私配置与授权要求。
- 备份保留期、删除信息保留策略和恢复流程。
- 云端套餐、月度预算、超量与到期处理。
- 首批试用参与者和发布验收记录方式。
