# PAOXIA 最小功能切片

## 切片 1：从首页查看真实的会话记录

**这一轮用户能做到什么**  
用户可以从首页直接进入 **Days Out.**；没有记录时看到空状态，完成跑步并保存后能看到本次记录。

**要做什么**

- 首页增加历史入口。
- 历史页优先展示当前会话产生的记录。
- 移除或明确隔离演示数据，避免把示例统计当成用户记录。
- 保持现有 Year / Month / Day 视觉结构。

**这轮明确不做什么**

- 不做本地持久化。
- 不做数据库、登录或云同步。
- 不做记录修改和删除。

**验收条件**

- 首次打开历史页时显示清晰的空状态。
- 完成一次跑步并保存后，Day 页显示本次真实时长和填写内容。
- Year / Month 不显示不存在的用户成绩。
- 用户可以从历史页通过 **Go again** 回到首页。
- 首页原有 **GO** 行为不受影响。

## 切片 2：沿途见闻支持多选

**这一轮用户能做到什么**  
用户可以同时选择多个沿途见闻，例如 Tree、Wind 和 Cloud，并再次点击取消其中一个。

**要做什么**

- 将当前单选见闻改成多选状态。
- 保存并展示多个已选见闻。
- 保留现有红色手绘选中圈和图标样式。

**这轮明确不做什么**

- 不增加新的见闻类型。
- 不做自由涂鸦和自定义标签。
- 不改天气和心情的选择规则。

**验收条件**

- 至少可以同时选择两个见闻。
- 再次点击已选项目可以取消。
- “Didn’t notice”与其他见闻不能产生矛盾状态。
- 保存后 Day 页完整展示所有已选见闻。
- 全部不选仍然可以保存。

## 切片 3：已完成记录保存在本机

**这一轮用户能做到什么**  
用户保存的多次跑步在关闭并重新打开小程序后仍然存在，并能在历史页查看。

**要做什么**

- 使用小程序本地 Storage 保存已完成记录。
- 历史页从本地记录生成 Year / Month / Day 内容。
- 支持保存多条记录，不覆盖上一条。
- 让首页历史入口始终可访问。

**这轮明确不做什么**

- 不接数据库和云同步。
- 不做微信账号身份识别。
- 不做跨设备找回。
- 不做同步状态和重试机制。

**验收条件**

- 连续保存两次跑步后，两条记录都能找到。
- 关闭并重新打开小程序后记录仍然存在。
- Year 和 Month 统计由本地真实记录计算。
- 没有距离、心情或备注的记录也能正常保存。
- 示例记录不会混入真实统计。

## 切片 4：未完成跑步可恢复

**这一轮用户能做到什么**  
计时过程中退出或关闭小程序，再次打开时可以继续同一次跑步，并保留正确时长和暂停状态。

**要做什么**

- 本地保存开始时间、暂停时间和累计暂停时长。
- 首页在存在未完成跑步时显示“继续这次”入口。
- 恢复时根据时间点重新计算时长。
- 正常结束后清除未完成状态。

**这轮明确不做什么**

- 不做跨设备继续计时。
- 不做云端计时。
- 不处理手机系统时间被手动修改的复杂纠错。
- 不做异常长时间提醒。

**验收条件**

- 计时中关闭小程序，重新打开后时长连续。
- 暂停时关闭小程序，重新打开后仍保持暂停。
- 暂停期间经过的时间不计入跑步时长。
- 完成并保存后，首页不再显示恢复入口。
- 点击新的 **GO** 不会意外覆盖未完成跑步。

## 切片 5：跑后填写草稿可恢复

**这一轮用户能做到什么**  
用户填写天气、心情、见闻、距离或备注时临时退出，回来后内容仍然保留。

**要做什么**

- 本地暂存跑后表单草稿。
- 重新进入跑后页面时恢复已填内容。
- 保存成功后清除草稿。
- 提供清晰的放弃草稿方式。

**这轮明确不做什么**

- 不同步草稿到云端。
- 不保留多个草稿。
- 不做自动保存提示或版本历史。

**验收条件**

- 填写至少三个字段后退出，重新进入时内容保持一致。
- 未填写字段仍保持空白。
- 保存后再次进入不会恢复旧草稿。
- 主动放弃后旧内容不再出现。
- 草稿不会被显示成已保存记录。

## 切片 6：结束后可以返回继续跑

**这一轮用户能做到什么**  
用户误点 **Done** 后，可以从跑后记录页回到原来的计时，并继续累计时长。

**要做什么**

- 跑后页增加“继续跑”入口。
- 将填写跑后表单的时间视为暂停时间。
- 返回跑步页时恢复结束前的计时状态。
- 再次结束时使用更新后的时长。

**这轮明确不做什么**

- 不做任意修改开始时间。
- 不做多段跑步合并。
- 不做复杂撤销历史。

**验收条件**

- 点击 **Done** 后可以返回跑步页。
- 跑后页面停留时间不计入跑步时长。
- 返回后可以继续暂停、恢复和结束。
- 再次结束时原有表单草稿仍然存在。
- 保存后不能再返回继续同一次跑步。

## 切片 7：修改一条已保存记录

**这一轮用户能做到什么**  
用户可以从 Day 详情页进入编辑，修改距离、心情、见闻和备注。

**要做什么**

- 在 Day 详情页提供编辑入口。
- 复用现有跑后表单。
- 编辑时加载原记录内容。
- 保存后返回更新后的详情页。

**这轮明确不做什么**

- 不修改原始开始时间和计时过程。
- 不做修改历史和撤销。
- 不做云端冲突处理。

**验收条件**

- 编辑页正确回显原记录。
- 修改备注后 Day 页立即显示新内容。
- 可以清空原有可选字段。
- 修改不会生成重复记录。
- 取消编辑不会改变原记录。

## 切片 8：删除一条已保存记录

**这一轮用户能做到什么**  
用户可以从 Day 详情页删除当前记录，并在确认后从历史统计中移除。

**要做什么**

- Day 详情页增加删除入口。
- 删除前提供原生确认提示。
- 删除后返回对应 Month 或空状态。
- 重新计算 Year / Month 统计。

**这轮明确不做什么**

- 不做回收站和恢复。
- 不做批量删除。
- 不做云端删除标记。
- 不做“删除全部数据”。

**验收条件**

- 取消确认时记录保持不变。
- 确认后该记录不再出现在历史页。
- 月份次数和总时长同步更新。
- 删除最后一条记录后显示空状态。
- 其他记录不受影响。

## 推荐现在先做：切片 1

当前主要流程已经可以完成计时、跑后填写和会话内数据传递，但历史页只能在保存后进入，而且演示数据看起来像真实用户记录。先完成“首页历史入口与真实空状态”，用户马上就能独立验收首页和历史之间的完整导航，也能确认哪些数据属于自己。

这一片不需要数据库或持久化，改动范围小，同时为后续本地保存、编辑和删除建立可信的界面入口。
